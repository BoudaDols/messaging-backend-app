/**
 * WebSocket typing indicator handlers.
 */

const logger = require("../utils/logger");

function typingHandler(io, socket) {
	// Placeholder — on implémentera dans la tâche 9.3
}

module.exports = typingHandler;
